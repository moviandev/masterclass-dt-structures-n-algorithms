// Unordered DS, key value pairs

let instructor = {
  firstname: "Kelly",
  isInstructor: true,
  favoriteNumbers: [1, 2, 3],
};

// Object.keys === O(n) time
// Object.values === O(n) time
// Object.entries === O(n) time
// Object.hasOwnProperty === O(1) time
